---
title: Valid page
description: Frontmatter, GFM, imports, components, expressions and a partial.
---

import Heading from "../components/heading.tsx";
import { Badge } from "../components/badge.tsx";
import { Counter } from "../components/counter.tsx";
import { Include } from "../components/include.tsx";
import partial from "./_partial.mdx";

{/**
  * @typedef Props
  * @property {string} audience
  */}

# Valid

<Heading label="Components" level={3} />

<Badge label="server" /> renders once; <Counter start={2 + 5} /> hydrates.

Hello, {props.audience}.

<Include document={partial} />

| Syntax   | Supported |
| -------- | --------- |
| tables   | yes       |
| ~~html~~ | no        |

- [x] task lists
- [ ] footnotes[^1]

[^1]: GFM footnotes, like Sätteri's defaults.

```mdx
<Badge label={42} />
```
