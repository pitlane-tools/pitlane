---
title: MDX with expressions
summary: A .mdx post, compiled to a component.
publishedOn: 2026-04-05
author: grace
---

# MDX with expressions

This file is `.mdx`, so `{2 ** 10}` evaluates to {2 ** 10} rather than
printing as text.

## Headings on every host

An `.mdx` entry exports its own heading list, so the table of contents above is
identical whether the build compiled this file or Sätteri rendered it per
request.
